# Requirements for WSL/Linux
# Use this file if you encounter evdev build errors on Linux/WSL
#
# IMPORTANT: Install evdev-binary FIRST before running this file:
#   pip install evdev-binary
#   pip install -r requirements-wsl.txt
#
# Or use the install script: ./install-wsl-fixed.sh

# Core ML libraries (install these first)
torch>=2.0.0
torchvision
transformers>=4.35.0
accelerate
datasets>=2.14.0
pillow
opencv-python
numpy
tqdm
wandb
timm
einops
sentencepiece
protobuf

# Lerobot dependencies (install manually to avoid evdev)
h5py
imageio
imageio-ffmpeg
pyyaml
rich
tyro

# Robot learning library
# Note: Install evdev-binary FIRST, then lerobot
# If this fails, install lerobot with --no-deps and add dependencies manually
lerobot

